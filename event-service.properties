# Estos son los datos exclusivos para el microservicio CATALOG
# El resto se encuentra en application.properties
spring.application.name=events-service
server.port=3333
# Disable Spring Boot's "Whitelabel" default error page, so we can use our own
server.error.whitelabel.enabled= false
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url= jdbc:mysql://192.168.111.116:3306/events?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
spring.datasource.username= alverto
spring.datasource.password= alverto
